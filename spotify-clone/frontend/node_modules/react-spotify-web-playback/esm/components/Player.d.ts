import * as React from 'react';
import { ComponentsProps } from '../types/common';
declare const Player: React.ForwardRefExoticComponent<Pick<ComponentsProps, string | number> & React.RefAttributes<HTMLDivElement>>;
export default Player;
